<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Page Title</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" type="text/css" media="screen" href="/css/main.css" />
</head>
<body>
    <header>
        <nav>
            <a href='/'>Home</a>
            <a href='/about'>About</a>
            <a href='/events'>Events</a>
            <a href='/services'>Orderves</a>
            <a href='/users/<%=userId%>'>Profile</a>
        </nav>
    </header>
    <main>
    <form action="/services/<%=service._id%>?_method=PUT" method="POST">
        Here will be a dropdown showing each type of skill they can choose
        Job Title: <input type="text" name="title" value="<%=service.title%>"><br/>
        Description: <input type="text" name="about" value="<%=service.about%>"><br/>
        Maybe loop through all the images to edit serparatly? so they can delete images from here<br/>
        Add Images (URL): <input type="text" name="images" value="">
        <% for (let i = 0; i < service.images.length; i++) {%>
            <img src="<%=service.images[i]%>"><br/>
        <%}%>
        
        
        
        <button type="submit">Edit skillset/chef/orderve</button>
    </form>
    <form action="/services/<%=service._id%>?_method=DELETE" method="POST">
        <button type="submit">Delete</button>
    </form>
    </main>
    
</body>
</html>