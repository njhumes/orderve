<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Page Title</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" type="text/css" media="screen" href="/css/main.css" />
</head>
<body>
    <header>
            <nav>
                <a href='/'>Home</a>
                <a href='/about'>About</a>
                <a href='/events'>Events</a>
                <a href='/services'>Orderves</a>
                <% if (currentSession.logged){%>
                    <a href='/users/<%=currentUserId%>'>Profile</a>
                    <a href='/auth/logout'>Logout</a>
                <%} else {%>
                    <a href='/auth/loginPage'>Login</a>
                <%}%>
            </nav>
    </header>
    <main>
        <h1>Make bid for: <%=event.title%></h1>
        <form action="/bids/events/<%=event._id%>" method="POST">
        Service Type:   <select name="title">
                        <% event.servicesNeeded.forEach((service)=>{ %>
                            <option value="<%=service%>"><%=service%></option>
                        <% }) %>
                        </select><br/>
        Bid Amount: <input type="number" name="bidAmount"> <br>
        Message: <input type="text" name="note"> <br>
        <button type="submit">Submit Bid</button>
        </form>
    </main>
</body>
</html>