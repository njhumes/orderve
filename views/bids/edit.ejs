<!DOCTYPE html>
<html>
<head>
    <% include ../partials/head %>
</head>
<body>
    <header>
        <% include ../partials/navbar %>
    </header>
    <main>
        <h1>Edit Bid for: <%=bid.title%> at <%=event.title%></h1>
        <form action="/bids/<%=bid._id%>?_method=PUT" method="POST">
        Service Type:   <select name="title">
                            <option value=''>None</option>
                            <option value="Chef">Chef</option>
                            <option value="Bartender">Bartender</option>
                        </select><br/>
        Bid Amount: <input type="number" name="bidAmount" value="<%=bid.bidAmount%>">
        Message: <input type="text" name="note" value="<%=bid.note%>">
        <button type="submit">Submit Bid</button>
        </form>
    </main>
    <% include ../partials/footer %>
</body>
</html>